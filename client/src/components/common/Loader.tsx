const Loader = () => {
	return (
		<section className="absolute top-0 right-0 flex justify-center w-full h-full place-items-center">
			<span className="loader"></span>
		</section>
	);
};

export default Loader;
